services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      # Compatibility: keep the existing database inside the persistent volume.
      POSTGRES_DB: lifepack
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
